[package]
name = "hls-server"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.9"
ffmpeg-next = { version = "8.1.0", features = ["codec", "format"] }
tokio = { version = "1.52.1", features = ["macros", "rt-multi-thread"] }
